server:
  port: 9998
spring:
  thymeleaf:
    mode: HTML5
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=UTC&characterEncoding=utf8&useSSL=true
    username: root
    password: 496179067@qq.com
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.gsein.blog.front.model
  config-location: classpath:mybatis-config.xml
